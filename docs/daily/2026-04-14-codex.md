# 2026-04-14 Codex

## 추가 진행 1

1. `EXP-230`에서 확인한 `product_control_motion` uplift를 benchmark 전용 코드에 두지 않고, 실제 `services/worker` renderer 경로로 옮겼습니다.
2. `services/worker/renderers/media.py`의 `render_video()`를 확장해서 static concat과 deterministic zoompan motion을 같은 API에서 처리하게 만들었습니다.
3. `services/worker/pipelines/generation.py`에는 `_resolve_renderer_motion_presets()`를 추가해, 현재는 `b_grade_fun`일 때만 motion preset이 붙도록 연결했습니다.
4. 또한 `render-meta.json`에 `rendererMotionMode`, `rendererMotionPresets`를 남기도록 바꿔서, 결과물 기준선이 무엇인지 나중에 바로 읽을 수 있게 했습니다.

## 추가 결과 1

1. `services/worker/tests/test_media_renderer.py`를 새로 추가했고, motion preset이 들어간 `render_video()` 렌더링 단위 테스트를 붙였습니다.
2. `services/worker/tests/test_generation_pipeline.py`에는 기존 `friendly` 경로가 여전히 `static_concat`으로 유지되는지 확인하는 검증과 `b_grade_fun` 기본 motion preset 매핑 검증을 추가했습니다.
3. 검증 커맨드는 아래 두 개로 마쳤습니다.
   - `python -m py_compile services/worker/renderers/media.py services/worker/pipelines/generation.py services/worker/tests/test_generation_pipeline.py services/worker/tests/test_media_renderer.py`
   - `uv run --project services/worker pytest services/worker/tests/test_media_renderer.py services/worker/tests/test_generation_pipeline.py -q`
4. 테스트 결과는 `23 passed`였습니다.

## 추가 진행 2

1. 테스트만으로 끝내지 않고, 실제 worker generation pipeline으로 `T02 / promotion / b_grade_fun` 샘플도 한 번 생성했습니다.
2. artifact는 `docs/experiments/artifacts/exp-231-b-grade-renderer-motion-bridge/` 아래에 남겼습니다.
3. sample summary 기준으로 이번 생성은 `rendererMotionMode = zoompan_control`로 기록됐고, preset은 `push_in_center -> push_in_top -> push_in_top -> push_in_center` 순서로 적용됐습니다.

## 추가 결과 2

1. 이제 `EXP-230`의 motion uplift는 benchmark 내부 아이디어가 아니라 실제 본선 renderer 후보 코드로 연결된 상태입니다.
2. 다만 현재 preset selector는 아직 generic baseline이며, `tray/full-plate`, `glass drink`, `bottle+glass`를 구분하는 lane-aware 규칙은 본선 renderer에 아직 옮기지 않았습니다.
3. 따라서 다음 질문은 `새 모델을 더 붙일지`가 아니라, `현재 renderer baseline이 실제 샘플군에서 어디까지 안정적으로 먹히는지`를 확인하는 쪽입니다.

## 추가 문서 1

1. `docs/experiments/EXP-231-b-grade-renderer-motion-bridge.md`

## 추가 진행 3

1. `EXP-231` 다음 단계로, generic motion만 붙인 상태에서 멈추지 않고 `shot-aware prepare/motion policy`를 실제 `b_grade_fun` renderer에 연결했습니다.
2. `services/worker/renderers/framing.py`를 새로 추가했고, benchmark script에 있던 shot classifier/prepare mode 규칙을 `numpy` 없이 pure-PIL로 옮겼습니다.
3. 이어서 `services/worker/pipelines/generation.py`에서는 `b_grade_fun` scene마다 `shotType`, `prepareMode`, `motionPreset`을 계산하는 `_resolve_bgrade_scene_policy()`를 추가했습니다.
4. 이 변경으로 drink lane은 `cover_top` 또는 `cover_bottom`, tray lane은 `cover_center`, preserve lane은 `contain_blur`를 기본으로 쓰도록 연결됐습니다.
5. 결과물 `render-meta.json`에도 `rendererFramingMode`, `rendererScenePolicies`가 남도록 확장했습니다.

## 추가 결과 3

1. 테스트는 다시 아래 기준으로 돌렸고 그대로 `23 passed`였습니다.
   - `python -m py_compile services/worker/renderers/framing.py services/worker/pipelines/generation.py services/worker/tests/test_generation_pipeline.py services/worker/tests/test_media_renderer.py`
   - `uv run --project services/worker pytest services/worker/tests/test_media_renderer.py services/worker/tests/test_generation_pipeline.py -q`
2. 추가로 `규카츠`, `맥주`, `커피`, `라멘` 네 샘플을 `T02 / promotion / b_grade_fun`으로 실제 생성해 `EXP-232` artifact를 남겼습니다.
3. `맥주`는 `cover_bottom + push_in_bottom`, `커피`는 `cover_top + push_in_top`, `규카츠`는 `cover_center` 중심, `라멘`은 `contain_blur`로 실제 분기되는 것을 확인했습니다.
4. 즉 drink/tray lane bridge는 실제 renderer에서도 살아났습니다.
5. 다만 `라멘` contact sheet를 보면 preserve lane의 `contain_blur`는 배경 blur 존재감이 다소 강해서, 여기만 후속 OVAT가 필요하다는 점도 같이 확인했습니다.

## 추가 문서 2

1. `docs/experiments/EXP-232-shot-aware-bgrade-renderer-policy-audit.md`

## 추가 진행 4

1. `EXP-232`에서 남아 있던 마지막 질문인 preserve lane을 바로 별도 OVAAT로 정리했습니다.
2. `라멘`, `순두부짬뽕`, `장어덮밥`, `귤모찌` 네 샘플을 대상으로 `contain_blur`와 `cover_center`를 직접 비교했습니다.
3. 비교 결과는 전반적으로 `cover_center`가 더 깔끔했고, `contain_blur`는 음식보다 blur backdrop 존재감이 먼저 들어와 서비스 baseline으로 보기 어려웠습니다.
4. 따라서 `EXP-233` 결론으로 preserve lane 기본값을 `cover_center`로 바꾸기로 결정했습니다.

## 추가 결과 4

1. 이 결론을 바로 `services/worker/pipelines/generation.py`에 반영했습니다.
2. 현재 preserve lane 정책은 아래처럼 정리됐습니다.
   - 기본 scene: `cover_center + push_in_center`
   - detail scene: `cover_center + push_in_top`
   - `period` scene: `cover_top + push_in_top`
3. 관련 테스트를 다시 돌렸고 여전히 `23 passed`였습니다.
4. 이어서 `EXP-234`로 실제 rollout check도 수행했고, `라멘`, `순두부짬뽕`, `장어덮밥`, `귤모찌` 네 샘플 모두 cleaner한 결과를 확인했습니다.
5. 즉 현재 본선 `b_grade_fun` baseline은 drink/tray/preserve 세 축 모두 shot-aware policy가 정리된 상태입니다.

## 추가 문서 3

1. `docs/experiments/EXP-233-preserve-shot-framing-policy-ovaat.md`
2. `docs/experiments/EXP-234-preserve-shot-cover-center-rollout-check.md`

## 추가 진행 5

1. preserve lane까지 정리한 뒤, 현재 shot-aware policy를 `T04 / review / b_grade_fun`에도 그대로 유지할 수 있는지 바로 확인했습니다.
2. `규카츠`, `맥주`, `커피`, `라멘` 샘플을 `review` 템플릿으로 실제 생성해 contact sheet를 비교했습니다.

## 추가 결과 5

1. `review` 템플릿에는 `period` scene이 없어서, 현재 policy가 오히려 더 단순하고 안정적으로 적용됐습니다.
2. `규카츠`는 `cover_center`, `맥주`는 `cover_bottom`, `커피`는 `cover_top`, `라멘`은 `cover_center` 중심으로 자연스럽게 유지됐습니다.
3. 따라서 현재 `b_grade_fun` shot-aware policy는 `T02 / promotion`뿐 아니라 `T04 / review`에서도 그대로 production baseline으로 유지 가능하다는 판단으로 정리했습니다.

## 추가 문서 4

1. `docs/experiments/EXP-235-review-template-shot-aware-policy-check.md`

## 추가 진행 6

1. `EXP-235`까지로 대표 4샘플 검증은 끝났지만, canonical sample pool까지 넓혀도 같은 baseline이 유지되는지와 helper-level parity는 아직 닫히지 않았습니다.
2. 실제로 `services/worker/renderers/framing.py`의 `choose_prepare_mode()`는 preserve shot에서 아직 `contain_blur`를 반환하고 있어서, 본선 policy와 helper 기준선이 어긋나 있는 상태였습니다.
3. 그래서 이번에는 새 모델이나 새 OVAAT를 더 붙이지 않고, preserve helper parity를 `cover_center`로 맞춘 뒤 `T02/T04 x 8샘플` 매트릭스를 한 번에 도는 재현용 스크립트를 추가했습니다.
4. 새 스크립트는 `scripts/shot_aware_renderer_baseline_matrix.py`이며, 실제 worker generation pipeline을 이용해 `promotion/review` 두 템플릿 기준 artifact를 재생산하도록 만들었습니다.

## 추가 결과 6

1. `규카츠`, `타코야키`, `맥주`, `커피`, `라멘`, `순두부짬뽕`, `장어덮밥`, `귤모찌`를 대상으로 총 `16 run`을 수행했습니다.
2. 결과 요약은 `docs/experiments/artifacts/exp-236-shot-aware-renderer-baseline-matrix/summary.json`에 남겼고, `T02`, `T04` 모두 first scene classifier parity가 `8/8`로 맞았습니다.
3. `tray/drink/preserve` 세 lane 모두 sample pool 확장 후에도 예상한 prepare/motion sequence를 유지했고, 대표 contact sheet 눈검사에서도 crop/blur 회귀는 보이지 않았습니다.
4. 따라서 현재 `b_grade_fun` shot-aware renderer baseline은 이제 freeze 가능한 수준으로 정리됐다고 판단했습니다.
5. 회귀 검증은 아래 기준으로 마쳤고 `23 passed`였습니다.
   - `python -m py_compile services/worker/renderers/framing.py services/worker/tests/test_generation_pipeline.py scripts/shot_aware_renderer_baseline_matrix.py`
   - `uv run --project services/worker pytest services/worker/tests/test_media_renderer.py services/worker/tests/test_generation_pipeline.py -q`

## 추가 문서 5

1. `docs/experiments/EXP-236-shot-aware-renderer-baseline-matrix.md`

## 추가 진행 7

1. `EXP-236` 이후 곧바로 다시 `실제 영상 생성 lane`을 열 수 있는지 확인하기 위해, 새 prompt OVAAT 대신 live benchmark 상태부터 다시 점검했습니다.
2. 비용과 시간을 줄이기 위해 이번에는 `맥주` 1샘플만 사용했고, 비교군은 `product_control_motion`, `veo31`, `sora2_current_best`, `manual_veo`로 고정했습니다.
3. 실행 커맨드는 아래와 같습니다.
   - `python scripts/video_upper_bound_benchmark.py --benchmark-id EXP-237-real-video-lane-reopen-beer-check --providers product_control_motion veo31 sora2_current_best manual_veo --images "docs\\sample\\음식사진샘플(맥주).jpg" --output-dir docs/experiments/artifacts/exp-237-real-video-lane-reopen-beer-check`

## 추가 결과 7

1. `product_control_motion`은 정상 완료됐고, `avg_rgb_diff=7.43`으로 static poster보다 motion 체감이 분명히 올라갔습니다.
2. 하지만 이 라인은 여전히 `controlled zoompan compositor`이므로, 실제 생성형 영상 주력이라기보다 fallback baseline으로 보는 편이 맞습니다.
3. `manual_veo`는 이번에도 `avg_rgb_diff=16.94`로 quality upper-bound reference 역할을 유지했습니다.
4. 반면 live provider는 둘 다 다시 막혔습니다.
   - `veo31`: `429 RESOURCE_EXHAUSTED`
   - `sora2_current_best`: `401 invalid_api_key`
5. 즉 지금 real video lane의 병목은 prompt나 motion phrase가 아니라 `provider access`입니다.
6. 따라서 다음 우선순위는 local/renderer 미세조정이 아니라, `OPENAI_API_KEY` 정상화와 `Veo quota` 확보 후 같은 benchmark를 재실행하는 쪽입니다.

## 추가 문서 6

1. `docs/experiments/EXP-237-real-video-lane-reopen-beer-check.md`

## 추가 진행 8

1. 사용자가 `OPENAI_API_KEY`를 새로 교체했다고 알려 주었기 때문에, `EXP-237`에서 막혔던 Sora live lane부터 다시 열었습니다.
2. 이번에는 비용을 줄이기 위해 `맥주` 1샘플만 사용했고, 비교군은 `product_control_motion`, `sora2_current_best`, `manual_veo`로 고정했습니다.
3. 실행 커맨드는 아래와 같습니다.
   - `python scripts/video_upper_bound_benchmark.py --benchmark-id EXP-238-sora2-lane-reopen-beer-check --providers product_control_motion sora2_current_best manual_veo --images "docs\\sample\\음식사진샘플(맥주).jpg" --output-dir docs/experiments/artifacts/exp-238-sora2-lane-reopen-beer-check`

## 추가 결과 8

1. `sora2_current_best`는 이번에 `94.09s` 만에 정상 완료됐고, 즉 `Sora live lane` 자체는 다시 operational 상태로 복귀했습니다.
2. 다만 `맥주` 샘플 기준 motion은 `avg_rgb_diff=7.91`로 `product_control_motion(7.43)`과 큰 차이가 없었고, contact sheet 체감도 `고품질 still + 약한 drift`에 가까웠습니다.
3. 반면 `manual_veo`는 계속 `avg_rgb_diff=16.94`로 upper-bound quality reference 역할을 유지했습니다.
4. 즉 key 문제는 해결됐지만, 그렇다고 `sora2_current_best`가 곧바로 본선 후보가 됐다고 보긴 어렵습니다.

## 추가 문서 7

1. `docs/experiments/EXP-238-sora2-lane-reopen-beer-check.md`

## 추가 진행 9

1. `EXP-238`만으로는 production 판단이 어려워서, 바로 `맥주 + 규카츠` 2샘플로 확장한 `EXP-239`를 수행했습니다.
2. 비교군은 그대로 `product_control_motion`, `sora2_current_best`, `manual_veo`를 유지했습니다.
3. 실행 커맨드는 아래와 같습니다.
   - `python scripts/video_upper_bound_benchmark.py --benchmark-id EXP-239-sora2-current-best-vs-control-two-sample-check --providers product_control_motion sora2_current_best manual_veo --images "docs\\sample\\음식사진샘플(맥주).jpg" "docs\\sample\\음식사진샘플(규카츠).jpg" --output-dir docs/experiments/artifacts/exp-239-sora2-current-best-vs-control-two-sample-check`

## 추가 결과 9

1. `맥주`에서는 `sora2_current_best`가 `avg_rgb_diff=6.76`으로 오히려 control보다도 낮았고, 체감상 near-static 성향이 더 강했습니다.
2. `규카츠`에서는 `sora2_current_best`가 `avg_rgb_diff=21.04`로 motion 수치는 크게 나왔지만, contact sheet를 보면 `사진 유지형 템플릿 광고`보다 `구도 재해석형 생성`에 더 가까웠습니다.
3. 즉 current best는 `좋은 모델인가`보다 `서비스가 원하는 형태로 좋은가`에서 아직 애매합니다.
4. 결론적으로 `Sora current best`는 두 샘플에서 일관된 production baseline을 만들지 못했고, 다음 질문은 `phrase`가 아니라 `input method` 쪽으로 넘어가야 한다는 쪽이 더 타당해졌습니다.

## 추가 문서 8

1. `docs/experiments/EXP-239-sora2-current-best-vs-control-two-sample-check.md`

## 추가 진행 10

1. 그래도 `phrase-level` 여지를 완전히 닫기 전에, 기존 `EXP-91` motion prompt family를 새 키 기준으로 한 번 더 rerun했습니다.
2. 실행은 기존 스크립트를 그대로 사용했고 artifact만 새 폴더로 분리했습니다.
3. 실행 커맨드는 아래와 같습니다.
   - `python scripts/sora2_motion_prompt_family_ovaat.py --output-dir docs/experiments/artifacts/exp-240-sora2-motion-prompt-family-rerun-after-key-restore`

## 추가 결과 10

1. `micro_motion_locked`는 `avg_rgb_diff=1.60`, `camera_orbit_beats`는 `avg_rgb_diff=1.61`로 둘 다 baseline reused(`16.72`) 대비 motion이 크게 줄었습니다.
2. `micro_motion_locked`는 보존성은 강했지만 사실상 정지컷에 가까웠고, `camera_orbit_beats`도 orbit/beats 지시가 실제 광고형 motion recovery로 이어지지 않았습니다.
3. 즉 key가 정상이어도 `phrase-level tuning`만으로는 `preserve + motion` trade-off를 못 푼다는 점이 다시 확인됐습니다.
4. 따라서 다음 active 실험은 `prompt phrase`가 아니라 `input framing`, `edit path`, `hybrid compositor packaging` 같은 입력/구조 변경 쪽이 맞습니다.

## 추가 문서 9

1. `docs/experiments/EXP-240-sora2-motion-prompt-family-rerun-after-key-restore.md`

## 추가 진행 11

1. `EXP-240`까지로 `prompt phrase` 축은 사실상 닫혔기 때문에, 다음 active 축을 `input framing`으로 옮겼습니다.
2. 다만 기존 `EXP-92`는 `맥주` 단일 샘플이었고, 지금 문제는 tray/full-plate의 재해석 리스크였기 때문에 이번에는 `규카츠` 기준 live OVAT를 따로 추가했습니다.
3. 새 스크립트 `scripts/sora2_input_framing_live_ovaat.py`를 만들어 `baseline_auto`, `hero_medium_zoom`, `hero_tight_zoom` 3개를 같은 prompt로 한 번에 실행할 수 있게 했습니다.

## 추가 결과 11

1. `baseline_auto` 첫 run은 `avg_rgb_diff=14.99`로 세 variant 중 motion이 가장 컸습니다.
2. `hero_medium_zoom`은 `6.47`, `hero_tight_zoom`은 `2.37`로 내려가면서, tighter crop이 tray lane에서도 motion을 줄이는 경향이 다시 나타났습니다.
3. 즉 `규카츠` 기준으로는 기존 `hero_tight current best` 가정이 오히려 약해졌습니다.

## 추가 진행 12

1. 그런데 이번 결과에서 더 큰 문제는 framing 차이보다 `repeatability`였습니다.
2. 같은 prepared input으로 `hero_tight_zoom`를 한 번 더 rerun했고, 이어서 `baseline_auto`도 같은 방식으로 repeat2를 실행했습니다.

## 추가 결과 12

1. `hero_tight_zoom`는 `EXP-239=21.04`, 이번 본 run=`2.37`, repeat2=`7.58`로 크게 흔들렸습니다.
2. `baseline_auto`도 첫 run `14.99`에서 repeat2 `4.20`으로 크게 내려갔습니다.
3. 즉 tray lane에서 지금 핵심 병목은 `어느 crop이 맞는가` 이전에, `같은 crop도 run마다 일관된 결과를 못 준다`는 점입니다.
4. 따라서 다음 우선순위는 `input framing` 추가가 아니라 `edit path` 또는 `generated shot + template packaging hybrid` 쪽으로 넘어가는 것이 맞습니다.

## 추가 문서 10

1. `docs/experiments/EXP-241-sora2-gyukatsu-input-framing-live-ovaat.md`

## 추가 진행 13

1. `EXP-241` 다음 축으로 `edit path`를 바로 tray lane에 적용했습니다.
2. 새 스크립트 `scripts/sora2_edit_live_ovaat.py`를 추가했고, `EXP-241 baseline_auto`를 source로 고정한 뒤 `same_shot_micro_motion`, `same_shot_push_in_motion` 두 edit를 live로 실행했습니다.

## 추가 결과 13

1. `same_shot_micro_motion`은 `avg_rgb_diff=15.32`로 source `14.99` 대비 사실상 gain이 거의 없었고, `mid-frame MSE`는 `+2025.81`로 preserve loss만 커졌습니다.
2. `same_shot_push_in_motion`은 더 직접적으로 motion을 요구했는데도 `avg_rgb_diff=9.31`로 오히려 source보다 내려갔습니다.
3. 즉 tray lane에서는 `edit path`가 motion recovery 해법이 아니라, source를 다시 흔드는 비효율 경로에 더 가까웠습니다.
4. 따라서 `edit`는 active 우선순위에서 내리고, 다음 축을 `hybrid packaging`으로 옮기는 편이 맞다고 정리했습니다.

## 추가 문서 11

1. `docs/experiments/EXP-242-sora2-gyukatsu-edit-live-ovaat.md`

## 추가 진행 14

1. `edit path`까지 약했기 때문에, 바로 `generated shot + template overlay` 하이브리드 proof를 만들었습니다.
2. 서비스 renderer 본체를 건드리지 않고, `scripts/generated_video_hybrid_packaging_proof.py`에서 generated mp4 위에 `HOOK / DETAIL / CTA` overlay를 시간대별로 얹는 proof를 추가했습니다.
3. source는 `EXP-241 baseline_auto / 규카츠` clip을 사용했습니다.

## 추가 결과 14

1. raw Sora clip 자체의 generation quality가 크게 좋아진 것은 아닙니다.
2. 하지만 `b_grade_fun` 오버레이를 얹은 hybrid packaged clip은 raw clip보다 `광고 결과물`로 인식되는 속도가 훨씬 빨랐고, 서비스 템플릿 문법과도 더 잘 맞았습니다.
3. 즉 현재 가장 현실적인 다음 축은 `더 많은 prompt`도, `더 많은 crop`도, `edit`도 아니라 `hybrid packaging pipeline` 쪽입니다.
4. 생성 quality 문제와 서비스 packaging 문제를 분리해서 다루는 편이 훨씬 생산적이라는 점이 확인됐습니다.

## 추가 문서 12

1. `docs/experiments/EXP-243-generated-video-hybrid-packaging-proof.md`

## 추가 진행 15

1. `EXP-243`까지는 하이브리드 방향이 맞는지만 본 상태였기 때문에, 이번에는 그 경로를 `services/worker/renderers` 안의 실제 재사용 코드로 올렸습니다.
2. `services/worker/renderers/media.py`에 `create_scene_overlay_image()`, `render_hybrid_video()`를 추가했고, generated mp4 위에 시간대별 overlay를 얹는 최소 bridge를 renderer 유틸로 만들었습니다.
3. 이어서 `scripts/generated_video_hybrid_packaging_proof.py`도 자체 ffmpeg 로직 대신 이 renderer 유틸을 직접 쓰도록 정리했습니다.

## 추가 결과 15

1. 이제 하이브리드 packaging은 실험용 proof script 내부 로직이 아니라, service renderer 코드 경로에서 재사용 가능한 기능이 됐습니다.
2. `services/worker/tests/test_media_renderer.py`에 hybrid overlay 테스트를 추가했고, `uv run --project services/worker pytest services/worker/tests/test_media_renderer.py -q` 결과는 `2 passed`였습니다.
3. 새 renderer utility 기반으로 `exp-244-hybrid-renderer-bridge-spike` artifact도 다시 생성했고, `hybrid_contact_sheet_8up.png` 기준 raw generated clip보다 광고 문법이 더 빠르게 읽히는 것을 재확인했습니다.
4. 즉 다음 단계는 더 많은 model OVAT가 아니라, `source mp4를 generation pipeline에 받는 최소 입구`를 만드는 쪽으로 넘어갈 수 있는 상태가 됐습니다.

## 추가 문서 13

1. `docs/experiments/EXP-244-hybrid-renderer-bridge-spike.md`

## 추가 진행 16

1. `EXP-244` 다음 단계로, 하이브리드 방향이 실제 generation pipeline 본선 경로에 들어갈 수 있는지 확인하기 위해 `hybrid source video` 입력 입구를 추가했습니다.
2. `services/worker/pipelines/generation.py`에서 `input_snapshot.hybridSourceVideoPath`를 읽도록 만들고, 이 값이 있으면 scene full image 대신 `scene overlay PNG`를 만들고 최종 렌더링을 `render_hybrid_video()`로 우회하도록 연결했습니다.
3. 동시에 `render-meta.json`에도 `rendererVideoSourceMode=hybrid_generated_clip`, `rendererMotionMode=hybrid_overlay_packaging`, `rendererHybridSourceVideo`, `rendererHybridOverlayTimeline`이 남도록 정리했습니다.
4. 재현용으로 `scripts/run_hybrid_generation_pipeline_spike.py`도 추가해, sample image + generated source mp4를 stage한 뒤 실제 generation run을 끝까지 실행하고 summary artifact를 남길 수 있게 했습니다.
5. 이어서 result surface도 최소한으로 연결했습니다. API result payload에 `rendererSummary`를 추가했고, web의 `ResultMediaPreview`에서 `source / motion / framing / duration` badge를 바로 보이게 했습니다.

## 추가 결과 16

1. 첫 spike 실행 자체는 성공했지만, 여기서 source generated clip은 약 `4.3초`인데 `T02` template timeline은 `6.4초`여서 뒤쪽 `PERIOD / CTA` overlay가 실제 최종 영상에서는 잘린다는 문제가 바로 드러났습니다.
2. 이 문제를 해결하기 위해 `services/worker/renderers/media.py`의 `render_hybrid_video()`에 `freeze_last_frame` 전략을 추가했고, source가 더 짧으면 마지막 프레임을 hold 해서 template 종료 시점까지 연장되도록 보정했습니다.
3. 이후 `services/worker/tests/test_media_renderer.py`, `services/worker/tests/test_generation_pipeline.py`에 duration probe 검증을 붙였고, worker 테스트는 다시 `25 passed`로 통과했습니다.
4. `python scripts/run_hybrid_generation_pipeline_spike.py` rerun 결과, 최종 hybrid output duration은 `6.3초`, meta상 target duration은 `6.4초`, strategy는 `freeze_last_frame`으로 기록됐고 `CTA`까지 보이는 contact sheet도 확보했습니다.
5. 즉 이제 하이브리드 방향은 `proof script`나 `renderer utility` 수준이 아니라, 실제 generation pipeline에서 generated mp4를 받아 광고 결과물로 끝낼 수 있는 baseline 후보가 됐습니다.
6. 추가로 `uv run --project services/api pytest services/api/tests/test_api_smoke.py -q`는 `5 passed`, `npm run build:web`도 성공해서 hybrid renderer summary가 API/web surface까지 깨지지 않고 연결되는 것을 확인했습니다.

## 추가 문서 14

1. `docs/experiments/EXP-245-hybrid-generation-pipeline-spike.md`

## 추가 진행 17

1. `EXP-245` 다음 우선순위였던 `hybrid source clip acceptance gate`를 실제 코드로 고정했습니다.
2. `scripts/video_quality_gate.py`를 추가해서 benchmark 결과의 `mid_frame_metrics.mse`, `motion_metrics.avg_rgb_diff`, `packaging_fit`, `provider` 기준으로 `native_control / accept / manual_review / reject`를 계산하게 했습니다.
3. 이 helper를 `scripts/video_upper_bound_benchmark.py`에도 연결해서, 앞으로 새 benchmark를 돌리면 top-level `summary.json`에 각 provider의 `hybrid_source_gate`와 aggregate summary가 자동으로 남도록 만들었습니다.
4. 이어서 `scripts/run_hybrid_source_gate_report.py`를 추가해 기존 `EXP-238`, `EXP-239` summary를 다시 읽어 calibration report를 생성할 수 있게 했습니다.

## 추가 결과 17

1. gate 기준은 아래처럼 고정했습니다.
   - preserve: `mse <= 3000 -> pass`, `<= 6500 -> hold`, 그 이상 `fail`
   - motion: `avg_rgb_diff < 6.5 -> fail`, `< 10 -> hold`, 그 이상 `pass`
2. `services/worker/tests/test_video_quality_gate.py`를 추가했고, `accept / manual_review / reject / native_control` 분기와 summary count 집계를 검증했습니다.
3. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile scripts/video_quality_gate.py scripts/video_upper_bound_benchmark.py scripts/run_hybrid_source_gate_report.py services/worker/tests/test_video_quality_gate.py`
   - `uv run --project services/worker pytest services/worker/tests/test_video_quality_gate.py -q`
   - `python scripts/run_hybrid_source_gate_report.py`
4. calibration report aggregate 기준으로는 `native_control=3`, `accept=2`, `manual_review=3`, `reject=1`이 나왔습니다.
5. 핵심 해석은 다음과 같습니다.
   - `sora2_current_best / 맥주`: `accept`
   - `sora2_current_best / 규카츠`: `manual_review`
   - `manual_veo / 규카츠`: `reject`
6. 추가로 `product_control_motion`만으로 `EXP-246-benchmark-annotation-smoke`를 짧게 rerun해, benchmark summary에도 `hybrid_source_gate`가 자동 annotation 되는 것을 확인했습니다.

## 추가 문서 15

1. `docs/experiments/EXP-246-hybrid-source-gate-calibration.md`

## 추가 진행 18

1. `EXP-246`의 gate를 실제 실행 경로에 연결하기 위해 `scripts/hybrid_source_selection.py`를 추가했습니다.
2. 이 helper는 benchmark `summary.json`을 읽고 provider별 `hybrid_source_gate` 기준으로 candidate를 수집한 뒤, 기본적으로 `accept`만 자동 선택하고 `manual_review`는 명시적 opt-in 없이는 막도록 만들었습니다.
3. 이어서 `scripts/run_hybrid_generation_pipeline_spike.py`를 manual mode + benchmark_gate mode 둘 다 지원하게 바꿨습니다.
4. benchmark_gate mode에서는 선택 provenance를 `hybridSourceSelection`으로 `input_snapshot_json`에 넣고, worker pipeline은 이를 `rendererHybridSourceSelection`으로 `render-meta.json`까지 전파하도록 확장했습니다.

## 추가 결과 18

1. `services/worker/tests/test_hybrid_source_selection.py`를 새로 추가했고, `accept 우선`, `manual_review 기본 차단`, `allow_manual_review 명시 시 허용`, `selection snapshot 생성`을 검증했습니다.
2. `services/worker/tests/test_generation_pipeline.py`의 hybrid case에도 selection provenance 전파 검증을 추가했습니다.
3. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile scripts/hybrid_source_selection.py scripts/run_hybrid_generation_pipeline_spike.py services/worker/pipelines/generation.py services/worker/tests/test_generation_pipeline.py services/worker/tests/test_hybrid_source_selection.py`
   - `uv run --project services/worker pytest services/worker/tests/test_media_renderer.py services/worker/tests/test_generation_pipeline.py services/worker/tests/test_video_quality_gate.py services/worker/tests/test_hybrid_source_selection.py -q`
4. 테스트 결과는 `33 passed`였습니다.
5. 실제 accepted path도 한 번 태워 봤습니다.
   - `python scripts/run_hybrid_generation_pipeline_spike.py --experiment-id EXP-247-hybrid-source-selection-flow-spike --output-dir docs/experiments/artifacts/exp-247-hybrid-source-selection-flow-spike --benchmark-summary docs/experiments/artifacts/exp-239-sora2-current-best-vs-control-two-sample-check/summary.json --label 맥주`
6. 이 실행에서는 `EXP-239 / 맥주 / sora2_current_best / accept`가 자동 선택됐고, summary와 render meta 모두에 `benchmarkId`, `provider`, `gateDecision=accept`가 남았습니다.
7. 반대로 `규카츠`를 같은 방식으로 넣으면 `manual_review`와 `reject`만 있어 기본 차단된다는 것도 smoke로 확인했습니다.
8. 즉 이제 hybrid lane은 `accept clip 자동 선택`, `manual_review clip 기본 차단`까지 포함한 실제 실행 흐름을 갖추게 됐습니다.

## 추가 문서 16

1. `docs/experiments/EXP-247-hybrid-source-selection-flow-spike.md`

## 추가 진행 19

1. `EXP-247` 다음 단계로 `manual_review` 상태를 실제 검토 대기열로 다루기 위해 `scripts/manual_review_queue.py`를 추가했습니다.
2. 이 helper는 benchmark summary에서 `manual_review` 후보만 추출하고, 기본적으로 `role=hybrid_source_candidate`만 queue에 남기며 `reference_only`는 제외합니다.
3. 이어서 `scripts/run_manual_review_queue_report.py`를 추가해 기존 `EXP-238`, `EXP-239` summary를 다시 읽고 `report.json`, `queue.md` artifact를 생성할 수 있게 했습니다.
4. `services/worker/tests/test_manual_review_queue.py`도 추가해서 reference-only 필터, include 옵션, report/markdown 생성을 검증했습니다.

## 추가 결과 19

1. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile scripts/manual_review_queue.py scripts/run_manual_review_queue_report.py services/worker/tests/test_manual_review_queue.py`
   - `uv run --project services/worker pytest services/worker/tests/test_manual_review_queue.py services/worker/tests/test_hybrid_source_selection.py services/worker/tests/test_video_quality_gate.py -q`
   - `python scripts/run_manual_review_queue_report.py`
2. 테스트 결과는 `11 passed`였습니다.
3. 기본 queue artifact 기준으로 실제 승격 검토 대상은 `1건`만 남았습니다.
4. 남은 항목은 `EXP-239 / 규카츠 / sora2_current_best / manual_review`였고, `recommendedAction=identity_review_then_promote_if_pass`로 정리됐습니다.
5. 반면 `manual_veo` 계열은 `reference_only`라 기본 queue에서는 제외됐습니다.
6. 즉 이제 `accept 자동 선택`, `manual_review queue`, `reference_only/reject 참고용`이라는 운영 구분이 코드와 artifact 양쪽에 생겼습니다.

## 추가 문서 17

1. `docs/experiments/EXP-248-manual-review-queue.md`

## 추가 진행 20

1. `EXP-248` 다음 단계로 `manual_review` 후보를 실제 사람이 판정할 수 있게 `scripts/manual_review_decision.py`를 추가했습니다.
2. 여기서는 queue entry를 review packet으로 바꾸고, 5개 checklist 항목과 `hold / promote / reject` 최종 결정을 기록할 수 있게 했습니다.
3. 이어서 `scripts/run_manual_review_decision_log.py`를 추가해 queue report를 읽고 candidate별 `review_packet.json`, `decision.json`, `decision.md` artifact를 생성할 수 있게 했습니다.
4. 실제로 현재 유일한 queue 후보인 `EXP-239 / 규카츠 / sora2_current_best`에 대해 contact sheet 기준 예비 판단 artifact도 생성했습니다.

## 추가 결과 20

1. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile scripts/manual_review_decision.py scripts/run_manual_review_decision_log.py services/worker/tests/test_manual_review_decision.py`
   - `uv run --project services/worker pytest services/worker/tests/test_manual_review_decision.py services/worker/tests/test_manual_review_queue.py services/worker/tests/test_hybrid_source_selection.py services/worker/tests/test_video_quality_gate.py -q`
   - `python scripts/run_manual_review_decision_log.py --summary-note "contact sheet 기준으로 메인 규카츠는 유지되지만 프레이밍이 더 타이트해져 주변 구성 보존은 추가 확인이 필요합니다." --check main_subject_identity=pass --check peripheral_layout=hold --check text_qr_intrusion=pass --check motion_packaging_fit=pass --check promotion_readiness=hold --check-note "peripheral_layout=프레이밍이 타이트해 원본의 주변 구성 일부가 잘립니다" --check-note "promotion_readiness=contact sheet 기준 예비판정이며 실제 video 확인이 추가로 필요합니다"`
2. 테스트 결과는 `15 passed`였습니다.
3. 예비 decision artifact 기준으로 `규카츠 / sora2_current_best`는 `finalDecision=hold`로 남겼습니다.
4. 세부 checklist는 `main_subject_identity/pass`, `peripheral_layout/hold`, `text_qr_intrusion/pass`, `motion_packaging_fit/pass`, `promotion_readiness/hold`였습니다.
5. 즉 이제 운영 경로는 `accept 자동 선택`, `manual_review queue`, `decision log`, 그 다음 `approve 후 입력 연결`까지 이어질 수 있는 상태가 됐습니다.

## 추가 문서 18

1. `docs/experiments/EXP-249-manual-review-decision-log.md`

## 추가 진행 21

1. `EXP-249` 다음 단계로 decision log를 실제 selection 경로에 다시 연결하기 위해 `scripts/manual_review_registry.py`를 추가했습니다.
2. 이 helper는 decision artifact 디렉터리에서 `decision.json`을 읽고 `hold / promote / reject` 집계와 `promote`된 candidate lookup을 제공합니다.
3. `scripts/hybrid_source_selection.py`도 확장해서 `manual_review_decisions_dir`가 있으면 `promote`된 manual-review 후보를 다시 선택 가능하게 했고, selection snapshot에 review provenance를 더 남기도록 했습니다.
4. `scripts/run_hybrid_generation_pipeline_spike.py`에는 `--manual-review-decisions-dir` 인자를 추가했습니다.

## 추가 결과 21

1. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile scripts/manual_review_registry.py scripts/run_manual_review_registry_report.py scripts/hybrid_source_selection.py scripts/run_hybrid_generation_pipeline_spike.py services/worker/tests/test_hybrid_source_selection.py services/worker/tests/test_manual_review_registry.py`
   - `uv run --project services/worker pytest services/worker/tests/test_manual_review_registry.py services/worker/tests/test_manual_review_decision.py services/worker/tests/test_manual_review_queue.py services/worker/tests/test_hybrid_source_selection.py services/worker/tests/test_video_quality_gate.py -q`
   - `python scripts/run_manual_review_registry_report.py`
   - `python scripts/run_hybrid_generation_pipeline_spike.py --experiment-id EXP-250-hold-registry-block-smoke --output-dir docs/experiments/artifacts/exp-250-hold-registry-block-smoke --benchmark-summary docs/experiments/artifacts/exp-239-sora2-current-best-vs-control-two-sample-check/summary.json --label 규카츠 --manual-review-decisions-dir docs/experiments/artifacts/exp-249-manual-review-decision-log`
2. 테스트 결과는 `18 passed`였습니다.
3. registry report 기준 현재 상태는 `entryCount=1`, `decisionCounts={\"hold\": 1}`, `promotedCandidateKeys=[]`였습니다.
4. 즉 현재 `규카츠 / sora2_current_best`는 decision log는 있지만 아직 `promote`가 아니므로 selection 경로에 복귀하지 않습니다.
5. block smoke도 실제로 `ValueError`로 끝나서, `hold` 후보가 잘못 자동 승격되지 않는 것을 확인했습니다.

## 추가 문서 19

1. `docs/experiments/EXP-250-manual-review-registry.md`

## 추가 진행 22

1. `규카츠 / sora2_current_best` hold 후보를 실제 hybrid packaging 결과 기준으로 다시 보기 위해 `EXP-251` review-only spike를 실행했습니다.
2. 여기서 final hybrid output 기준 `hybrid_contact_sheet.png`, `first_frame.png`, `mid_frame.png`, `original_vs_hybrid_mid.png`를 추가로 만들어 원본 대비 구도 변화와 최종 광고 읽힘을 함께 확인했습니다.
3. 그 결과를 바탕으로 `run_manual_review_decision_log.py`를 다시 실행해 해당 후보를 `finalDecision=promote`로 갱신했습니다.
4. 이어서 `run_manual_review_registry_report.py`를 다시 돌려 promoted registry를 재생성했고, `EXP-252` selection smoke로 registry 경유 선택까지 다시 검증했습니다.

## 추가 결과 22

1. `EXP-251` 기준으로 메인 규카츠와 주요 그릇 구성은 유지됐고, 원본보다 crop이 타이트하지만 text/QR intrusion 없이 최종 광고 읽힘이 충분하다고 판단했습니다.
2. 이에 따라 `EXP-239 / 규카츠 / sora2_current_best` decision log는 `finalDecision=promote`, `statusCounts={pass: 5}`로 갱신됐습니다.
3. registry report도 즉시 바뀌었고, 현재 상태는 `entryCount=1`, `decisionCounts={\"promote\": 1}`, `promotedCandidateKeys=[\"EXP-239-sora2-current-best-vs-control-two-sample-check::규카츠::sora2_current_best\"]`입니다.
4. `EXP-252` selection smoke는 정상 완료됐고, summary 기준 `hybridSourceSelection.selectionMode=promoted_manual_review`, `reviewFinalDecision=promote`, `finalVideoDurationSec=6.3`으로 남았습니다.
5. 즉 `manual_review -> promote -> registry -> selection` 경로가 실제 artifact 기준으로 닫혔습니다.

## 추가 문서 20

1. `docs/experiments/EXP-251-manual-review-hybrid-packaging-check.md`
2. `docs/experiments/EXP-252-promoted-manual-review-selection-smoke.md`

## 추가 진행 23

1. `EXP-252` 다음 단계로 approved candidate를 실제 API generate 경로에 태우기 위해 `services/api` 쪽 입력 경로를 확장했습니다.
2. `GenerateRequest`, `RegenerateRequest`에 `approvedHybridSourceCandidateKey`를 추가했고, 라우트도 `dict` 대신 실제 request schema를 사용하도록 정리했습니다.
3. `runtime.py`에서는 promoted decision artifact를 읽어 source video를 project storage로 stage하고, `input_snapshot.hybridSourceVideoPath`, `input_snapshot.hybridSourceSelection`을 자동 구성하도록 붙였습니다.
4. result API `rendererSummary`에도 `hybridSourceSelectionMode`, `hybridSourceCandidateKey`를 노출해서 결과가 어떤 hybrid provenance로 만들어졌는지 확인할 수 있게 했습니다.

## 추가 결과 23

1. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile services/api/app/core/config.py services/api/app/schemas/api.py services/api/app/api/routes.py services/api/app/services/runtime.py services/api/tests/test_api_smoke.py`
   - `uv run --project services/worker pytest services/api/tests/test_api_smoke.py -q`
2. 테스트 결과는 `6 passed`였습니다.
3. `docs/experiments/artifacts/exp-253-approved-hybrid-source-api-path/summary.json` 기준으로 `status=generated`, `rendererSummary.videoSourceMode=hybrid_generated_clip`, `motionMode=hybrid_overlay_packaging`, `hybridSourceSelectionMode=promoted_manual_review`가 남았습니다.
4. 즉 `decision log -> approved candidate key -> API generate -> worker hybrid render` 경로가 실제 artifact 기준으로 닫혔습니다.
5. 이제 approved candidate는 spike 전용 메모가 아니라, 본선 API 입력에서 재사용 가능한 baseline 단위가 됐습니다.

## 추가 문서 21

1. `docs/experiments/EXP-253-approved-hybrid-source-api-path.md`

## 추가 진행 24

1. `EXP-253` 다음 단계로 `accept`와 `promote`를 한 군데서 볼 수 있는 approved inventory를 만들기 위해 `scripts/approved_hybrid_inventory.py`를 추가했습니다.
2. 이 helper는 benchmark summary에서 `accept`된 hybrid source 후보를 모으고, manual review decision log에서 `promote`된 후보를 함께 합쳐 lane별 approved inventory를 만듭니다.
3. 이어서 `scripts/run_approved_hybrid_inventory_report.py`를 추가해 `EXP-238`, `EXP-239`, `EXP-249` 기준 inventory artifact를 실제로 생성할 수 있게 했습니다.
4. 테스트로는 `services/worker/tests/test_approved_hybrid_inventory.py`를 추가해서 `benchmark_accept`와 `manual_review_promote`가 하나의 approved inventory로 합쳐지는지 검증했습니다.

## 추가 결과 24

1. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile scripts/approved_hybrid_inventory.py scripts/run_approved_hybrid_inventory_report.py services/worker/tests/test_approved_hybrid_inventory.py`
   - `uv run --project services/worker pytest services/worker/tests/test_approved_hybrid_inventory.py services/worker/tests/test_manual_review_registry.py services/worker/tests/test_hybrid_source_selection.py -q`
   - `python scripts/run_approved_hybrid_inventory_report.py`
2. 테스트 결과는 `8 passed`였습니다.
3. inventory artifact 기준 현재 approved 후보는 `3건`이며, `laneCounts={"drink_glass_lane": 2, "tray_full_plate_lane": 1}`입니다.
4. 추천 후보는 `drink_glass_lane -> EXP-238 / 맥주 / sora2_current_best / benchmark_accept`, `tray_full_plate_lane -> EXP-239 / 규카츠 / sora2_current_best / manual_review_promote`로 남았습니다.
5. 즉 이제 기준선은 `drink lane은 gate accept 중심`, `tray/full-plate lane은 promote review 중심`으로 문서상도 고정됐습니다.

## 추가 문서 22

1. `docs/experiments/EXP-254-approved-hybrid-candidate-inventory.md`

## 추가 진행 25

1. `EXP-254`에서 만든 approved inventory를 실제 서비스가 읽을 수 있게 하기 위해 API read path를 추가했습니다.
2. `APP_APPROVED_HYBRID_INVENTORY_REPORT_PATH` 설정을 추가했고, runtime helper가 inventory report artifact를 읽어 `label`, `serviceLane` 기준으로 필터링하도록 정리했습니다.
3. 라우트로는 `GET /api/hybrid-candidates/approved`를 추가했고, API 스모크 테스트에도 inventory 조회 케이스를 붙였습니다.
4. 이어서 실제 `EXP-254` artifact를 읽는 response artifact도 `EXP-255`로 남겼습니다.

## 추가 결과 25

1. 검증 커맨드는 아래와 같습니다.
   - `python -m py_compile services/api/app/core/config.py services/api/app/schemas/api.py services/api/app/api/routes.py services/api/app/services/runtime.py services/api/tests/test_api_smoke.py`
   - `uv run --project services/worker pytest services/api/tests/test_api_smoke.py -q`
2. 테스트 결과는 `7 passed`였습니다.
3. `docs/experiments/artifacts/exp-255-approved-hybrid-inventory-api-read-path/summary.json` 기준 full inventory 응답은 `itemCount=3`, `laneCounts={"drink_glass_lane": 2, "tray_full_plate_lane": 1}`였습니다.
4. lane 추천 응답은 `drink_glass_lane -> EXP-238 / 맥주 / sora2_current_best`, `tray_full_plate_lane -> EXP-239 / 규카츠 / sora2_current_best`로 그대로 읽혔습니다.
5. 즉 이제 approved inventory는 artifact에만 머무는 게 아니라 API에서 바로 소비 가능한 기준선이 됐습니다.

## 추가 문서 23

1. `docs/experiments/EXP-255-approved-hybrid-inventory-api-read-path.md`

## 추가 진행 26

1. `EXP-255` 다음 단계로 approved inventory를 실제 web generate 흐름에서 소비할 수 있게 연결했습니다.
2. `packages/contracts/schemas/generation.ts`에 `approvedHybridSourceCandidateKey`와 hybrid renderer summary 필드를 추가해 API 응답/요청과 web contract를 맞췄습니다.
3. `apps/web/src/app/api/hybrid-candidates/approved/route.ts`를 추가해 local demo 환경에서도 `EXP-254` inventory artifact를 그대로 읽어 `/api/hybrid-candidates/approved`를 제공하도록 했습니다.
4. `apps/web/src/components/demo-workbench.tsx`에는 승인된 hybrid 기준선 선택 UI를 붙여 generate/regenerate 요청에 선택된 candidate key를 함께 보낼 수 있게 했습니다.
5. `apps/web/src/lib/demo-store.ts`도 initial generation에서 approved candidate key를 보존하고, result `rendererSummary`가 hybrid source 사용 여부를 반영하도록 맞췄습니다.

## 추가 결과 26

1. 검증 커맨드는 아래와 같습니다.
   - `npm run lint:web`
   - `npm run build:web`
2. 타입 체크와 Next production build가 모두 통과했습니다.
3. build 결과 route 목록에 `ƒ /api/hybrid-candidates/approved`가 새로 포함됐습니다.
4. 즉 이제 approved inventory는 `artifact -> API -> web generate UI -> result surface`까지 한 번에 이어지는 기준선이 됐습니다.
5. 이번 단계에서는 lane 자동 추론이나 자동 선택은 의도적으로 넣지 않았고, 운영자가 명시적으로 candidate를 선택하는 형태로만 닫았습니다.

## 추가 문서 24

1. `docs/experiments/EXP-256-approved-hybrid-inventory-web-consumption.md`

## 추가 진행 27

1. `EXP-256` 다음 단계로 approved inventory 선택 부담을 줄이기 위해 lane hint / lane filter / 추천 candidate 적용 UI를 추가했습니다.
2. `apps/web/src/components/demo-workbench.tsx`에 `inferHybridServiceLaneHint()`를 넣었고, 현재 임시 규칙은 `cafe -> drink_glass_lane`, `restaurant -> tray_full_plate_lane`입니다.
3. 이 규칙은 자동 attach가 아니라 UI hint로만 쓰이도록 했고, 운영자가 `추천 candidate 적용` 버튼을 눌러야 실제 candidate key가 선택되게 유지했습니다.
4. lane filter를 켜면 candidate select도 해당 lane 목록만 보도록 좁혀서, approved baseline 탐색 속도를 높였습니다.

## 추가 결과 27

1. 검증 커맨드는 아래와 같습니다.
   - `npm run lint:web`
   - `npm run build:web`
2. 타입 체크와 production build가 모두 다시 통과했습니다.
3. 즉 approved inventory web 소비 경로는 이제 `전체 목록 -> lane별 탐색 -> 추천 candidate 적용` 단계까지 확장됐습니다.
4. 다만 현재 lane hint는 businessType 기반 임시 규칙이므로, 자동 선택 로직으로 승격하기에는 아직 근거가 부족합니다.

## 추가 문서 25

1. `docs/experiments/EXP-257-approved-hybrid-lane-hint-ui.md`

## 추가 진행 28

1. `EXP-257` 다음 단계로 lane hint를 단순 businessType 규칙에서 evidence-based scoring helper로 확장했습니다.
2. `apps/web/src/lib/hybrid-lane-hint.ts`를 추가했고, `businessType`, `templateId`, `assetFileNames`, `approvedInventoryLabels`를 함께 점수화하도록 정리했습니다.
3. 특히 파일명에 approved inventory label이 직접 포함되면 가장 강한 신호로 lane을 잡고, 일반 drink/tray 키워드는 보조 신호로만 사용합니다.
4. `apps/web/src/components/demo-workbench.tsx`는 이제 이 helper의 `confidence`, `evidence[]`, `consideredAssetFileNames`를 화면에 함께 보여줍니다.

## 추가 결과 28

1. 검증 커맨드는 아래와 같습니다.
   - `npm run lint:web`
   - `npm run build:web`
2. 타입 체크와 production build가 모두 통과했습니다.
3. 즉 lane hint는 이제 `업종`만 보는 것이 아니라, `approved inventory label + 파일명 + 업종/템플릿` 근거를 같이 반영하는 단계까지 올라왔습니다.
4. 다만 이 단계에서도 candidate auto-attach는 하지 않았고, 근거가 약하면 `명확한 lane hint 없음` 상태를 유지합니다.

## 추가 문서 26

1. `docs/experiments/EXP-258-evidence-based-hybrid-lane-hint.md`
