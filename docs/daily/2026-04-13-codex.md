# 2026-04-13 Codex 작업 일지

## 오늘의 목표

- `prompt baseline`의 남은 `P2`를 하나 더 줄이기
- `new_menu highlightPrice=true`를 숫자 가격 환각 없이 안전하게 해석하는 기준을 만들기

## 진행 내용

1. OpenAI 고정 실험 경로가 정상 동작하는 상태에서 다음 `P2` 후보를 `new_menu / highlightPrice=true / emphasizeRegion=true`로 고정했습니다.
2. `services/worker/experiments/prompt_harness.py`에 `scenario-cafe-new-menu-fixed-highlight-price`와 `EXP-165`를 추가했습니다.
3. 이번 prompt는 `highlightPrice=true`를 숫자 가격 생성이 아니라 `difference/subText 가치 포인트 강조`로 해석하도록 설계했습니다.
4. baseline/candidate single run은 모두 `100.0`을 기록했고, repeatability도 둘 다 `3/3 pass`를 유지했습니다.
5. candidate는 baseline보다 `difference/subText`에서 메뉴 조합과 페어링 이유를 더 분명하게 적는 경향을 보였습니다.
6. 이어서 `new_menu_friendly_region_anchor_highlight_price` profile을 manifest에 추가하고 snapshot `accepted=true`를 확인했습니다.
7. worker runtime summary도 해당 조건에서 `option_match`로 새 profile을 추천했습니다.
8. `EXP-167`, `EXP-168`까지 다시 돌린 결과 coverage는 `option_match=8`, `coverage_gap=15`, priority는 `P2=3`, `P3=7`, `P4=5`가 됐습니다.

## 결과

1. `new_menu highlightPrice=true / emphasizeRegion=true` 공백 1건이 실제로 메워졌습니다.
2. 이제 최상위 `P2`는 `new_menu highlightPrice + shorterCopy combined` 1건과 `review` 2건입니다.
3. `new_menu` 축에서 숫자 가격이 없는 입력에도 `highlightPrice`를 안전하게 해석하는 기준이 생겼습니다.

## 다음 작업

1. 다음 최우선은 `new_menu / highlightPrice=true / shorterCopy=true / emphasizeRegion=true` combined profile입니다.
2. 그 다음은 `review / shorterCopy=false`, `review / highlightPrice=true` 단일 토글 2건입니다.
3. `new_menu` 축은 계속 `gpt-5-mini`로 가도 되고, 현재 범위에서는 새 provider 비교보다 coverage completion이 더 우선입니다.

## 추가 진행

1. 바로 이어서 남은 `new_menu combined P2`인 `highlightPrice=true + shorterCopy=true + emphasizeRegion=true`를 `EXP-169`로 진행했습니다.
2. 이번 prompt는 `짧게 쓰기`와 `value emphasis`를 동시에 만족시키기 위해 각 surface에 한 가지 가치 포인트만 남기도록 설계했습니다.
3. baseline/candidate single run은 모두 `100.0`, repeatability도 둘 다 `3/3 pass`를 기록했습니다.
4. candidate는 hook 길이를 더 짧게 유지하면서 `difference/subText`를 한 줄 가치 포인트로 압축했습니다.
5. 이어서 `new_menu_friendly_region_anchor_highlight_price_shorter_copy` profile을 manifest에 추가했고, `EXP-170` snapshot `accepted=true`, runtime `option_match`를 확인했습니다.
6. `EXP-171`, `EXP-172`까지 다시 돌린 결과 coverage는 `option_match=9`, `coverage_gap=14`, priority는 `P2=2`, `P3=8`, `P4=4`가 됐습니다.

## 추가 결과

1. `new_menu` 비지역 `P2`는 이번 세션에서 모두 정리됐습니다.
2. 남은 최상위 `P2`는 이제 `review / shorterCopy=false`, `review / highlightPrice=true` 2건뿐입니다.
3. 다음 세션은 `review` 축으로 넘어가는 편이 맞습니다.

## 추가 진행 3

1. 이어서 남은 `review` 축 `P2` 중 `highlightPrice=false / shorterCopy=false / emphasizeRegion=false`를 `EXP-173`으로 먼저 진행했습니다.
2. 이번 prompt는 `shorterCopy=false`를 문장 길이 전체 확장이 아니라, `subText`에서 식감/풍미/재방문 이유를 한 줄 더 설명하는 방식으로 고정했습니다.
3. baseline/candidate single run은 모두 `100.0`, repeatability도 둘 다 `3/3 pass`를 기록했습니다.
4. candidate는 hook 길이를 늘리지 않으면서 `subText.review_quote`, `subText.product_name`에만 설명을 더 실어 `shorterCopy=false` 의미를 더 정확히 반영했습니다.
5. 이어서 `review_strict_fallback_surface_lock_shorter_copy_off` profile을 manifest에 추가했고, `EXP-174` snapshot `accepted=true`, runtime `option_match`, worker `8 passed`, api `5 passed`를 확인했습니다.
6. `EXP-175`, `EXP-176`까지 다시 돌린 결과 coverage는 `option_match=10`, `coverage_gap=13`, priority는 `P2=2`, `P3=9`, `P4=2`가 됐습니다.

## 추가 결과 3

1. `review / shorterCopy=false / emphasizeRegion=false` 공백 1건이 실제로 메워졌습니다.
2. 남은 최상위 `P2`는 이제 둘 다 `review highlightPrice=true` 축입니다.
3. 다음 우선순위는 `review / highlightPrice=true / shorterCopy=false / emphasizeRegion=false`를 먼저 메우고, 그 다음 `shorterCopy=true` 조합을 정리하는 흐름이 가장 자연스럽습니다.

## 추가 진행 4

1. 이어서 `review` 축 남은 `P2` 두 건을 연속으로 마저 정리했습니다.
2. 먼저 `EXP-177`에서 `highlightPrice=true / shorterCopy=false / emphasizeRegion=false`를 추가했고, 이번 prompt는 `highlightPrice=true`를 가격 숫자 생성이 아니라 `한 끼 만족감/다시 갈 이유 foreground`로 해석하도록 고정했습니다.
3. baseline/candidate single run은 모두 `100.0`, repeatability도 둘 다 `3/3 pass`를 기록했습니다.
4. 이어서 `review_strict_fallback_surface_lock_highlight_price_shorter_copy_off` profile을 manifest에 추가했고, `EXP-178` snapshot `accepted=true`, runtime `option_match`, worker `9 passed`, api `5 passed`를 확인했습니다.
5. `EXP-179`, `EXP-180`까지 다시 돌린 결과 coverage는 `option_match=11`, `coverage_gap=12`, priority는 `P2=1`, `P3=10`, `P4=1`이 됐습니다.
6. 마지막으로 `EXP-181`에서 `highlightPrice=true / shorterCopy=true / emphasizeRegion=false`를 추가했고, 이번 prompt는 같은 value foreground를 더 짧은 한 줄 만족감 근거로 압축하는 방향으로 설계했습니다.
7. baseline/candidate single run은 다시 모두 `100.0`, repeatability도 둘 다 `3/3 pass`를 기록했습니다.
8. 이어서 `review_strict_fallback_surface_lock_highlight_price` profile을 manifest에 추가했고, `EXP-182` snapshot `accepted=true`, runtime `option_match`, worker `10 passed`, api `5 passed`를 확인했습니다.
9. `EXP-183`, `EXP-184`까지 다시 돌린 결과 coverage는 `option_match=12`, `coverage_gap=11`, priority는 `P3=11`만 남았습니다.

## 추가 결과 4

1. 비지역 단일 quick option `P2`는 이번 세션에서 모두 정리됐습니다.
2. 현재 남은 gap은 전부 `emphasizeRegion`이 포함된 `P3` 축입니다.
3. 따라서 지금 상태를 `비지역 baseline 정리 완료`로 보고, 다음 세션은 `region emphasis 확장 여부`를 따로 판단하는 편이 맞습니다.

## 추가 진행 5

1. 위 상태를 그대로 두지 않고, `new_menu / emphasizeRegion=false`를 `region 삭제`가 아니라 `caption 1개 + #성수동 1개`만 남기는 `minimal region anchor`로 다시 정의했습니다.
2. 먼저 `EXP-185`에서 `highlightPrice=false / shorterCopy=false / emphasizeRegion=false`를 열었고, 첫 draft는 hashtag 수를 1개로 줄여 repeatability가 흔들렸지만, `총 5~8개 hashtag 유지` 제약을 추가한 뒤 baseline/candidate 모두 single/repeatability `100.0`, `3/3 pass`를 기록했습니다.
3. 이어서 `EXP-186`으로 `new_menu_friendly_minimal_region_anchor` profile을 manifest에 추가했고, snapshot `accepted=true`, runtime `option_match`, worker `11 passed`, api `5 passed`를 확인했습니다.
4. `EXP-187`, `EXP-188` 기준 coverage는 `option_match=13`, `coverage_gap=10`, `exact_match=14`가 됐고, 남은 비지역 `P2`는 `shorterCopy=true`, `highlightPrice=true` 두 건으로 재정렬됐습니다.

## 추가 진행 6

1. 다음으로 `EXP-189`에서 `shorterCopy=true / emphasizeRegion=false`를 minimal anchor 축으로 확장했고, baseline/candidate 모두 single/repeatability `100.0`, `3/3 pass`를 기록했습니다.
2. 같은 흐름으로 `EXP-193`에서 `highlightPrice=true / emphasizeRegion=false`를 숫자 가격 환각 없는 value foreground로 확장했고, 역시 baseline/candidate 모두 single/repeatability `100.0`, `3/3 pass`를 유지했습니다.
3. `EXP-190`, `EXP-194` snapshot까지 반영한 뒤 coverage를 다시 집계한 `EXP-195`, `EXP-196` 결과는 `option_match=15`, `coverage_gap=8`, `exact_match=16`, `bandCounts={ P2: 1, P3: 7 }`였습니다.
4. 즉 non-region 쪽에서는 combined `highlightPrice=true + shorterCopy=true + emphasizeRegion=false` 한 건만 남게 됐습니다.

## 추가 진행 7

1. 마지막 non-region `P2`를 닫기 위해 `EXP-197`에서 `highlightPrice=true + shorterCopy=true + emphasizeRegion=false` combined minimal anchor를 진행했고, baseline/candidate 모두 single/repeatability `100.0`, `3/3 pass`를 기록했습니다.
2. 이어서 `EXP-198` snapshot `accepted=true`, runtime `option_match`, worker `14 passed`, api `5 passed`를 확인했습니다.
3. 최종 coverage 재집계 `EXP-199`, `EXP-200` 결과는 `option_match=16`, `coverage_gap=7`, `exact_match=17`, `bandCounts={ P3: 7 }`입니다.
4. 즉 `new_menu`의 비지역 quick option 조합은 이번 세션에서 모두 exact option match로 정리됐고, 남은 gap은 전부 region emphasis 계열입니다.

## 현재 기준 정리

1. `new_menu / emphasizeRegion=false`는 현재 `minimal region anchor` 해석이 baseline입니다.
2. 이 해석은 region을 완전히 지우는 것이 아니라, rule의 최소치를 맞추기 위해 `caption 1개 + #성수동 1개`만 남기고 hook/body foreground에서는 지역을 빼는 방식입니다.
3. 따라서 다음 세션의 선택지는 사실상 둘 중 하나입니다.
4. `promotion` region emphasis 3건을 먼저 열 것인지, `review` region emphasis 4건을 먼저 열 것인지 정하면 됩니다.

## 추가 진행 8

1. 위에서 남겨 둔 `promotion region emphasis` 3건을 이번에 연속으로 닫았습니다.
2. 먼저 `EXP-201`에서 `highlightPrice=false / shorterCopy=true / emphasizeRegion=true`를 열었고, baseline/candidate single run은 둘 다 `100.0`, repeatability도 `3/3 pass`를 기록했습니다.
3. 이어서 `promotion_surface_lock_highlight_price_off_region_anchor` profile을 manifest에 추가했고, `EXP-202` snapshot `accepted=true`, runtime `option_match`, transport fallback `timeout_90s_retry1`, worker `17 passed`, api `5 passed`를 확인했습니다.
4. 다음으로 `EXP-205`에서 `highlightPrice=true / shorterCopy=false / emphasizeRegion=true`를 열었고, baseline/candidate single run과 repeatability 모두 다시 `100.0`, `3/3 pass`를 기록했습니다.
5. 이어서 `promotion_surface_lock_shorter_copy_off_region_anchor` profile을 추가했고, `EXP-206` snapshot `accepted=true`, runtime `option_match`, transport fallback `timeout_90s_retry1`를 확인했습니다.
6. 마지막으로 `EXP-209`에서 default promotion region 조합 `highlightPrice=true / shorterCopy=true / emphasizeRegion=true`를 열었습니다.
7. 이 케이스는 첫 `90초 / retry 1회` single run에서 candidate가 timeout을 한 번 보였고, prompt 수정 없이 `120초 / retry 1회`로 다시 실행했을 때 baseline/candidate 모두 `100.0`으로 통과했습니다.
8. `120초 / retry 1회` repeatability에서도 baseline/candidate 모두 `3/3 pass`를 기록했고, 이후 `promotion_surface_lock_region_anchor` profile을 추가한 `EXP-210` snapshot도 `accepted=true`로 통과했습니다.
9. 세 profile을 모두 반영한 뒤 `EXP-211`, `EXP-212`를 다시 돌린 결과 coverage는 `option_match=19`, `coverage_gap=4`, `exact_match=20`, priority는 `P3=4`만 남았습니다.
10. 즉 이번 라운드로 `promotion` quick option 축은 `emphasizeRegion=true`까지 전부 option profile로 정리됐고, 남은 gap은 전부 `review region emphasis` 4건입니다.

## 추가 문서 8

1. `docs/experiments/EXP-201-gemma-promotion-highlight-price-off-region-anchor.md`
2. `docs/experiments/EXP-202-promotion-surface-lock-highlight-price-off-region-anchor-profile-snapshot.md`
3. `docs/experiments/EXP-205-gemma-promotion-shorter-copy-off-region-anchor.md`
4. `docs/experiments/EXP-206-promotion-surface-lock-shorter-copy-off-region-anchor-profile-snapshot.md`
5. `docs/experiments/EXP-209-gemma-promotion-region-anchor.md`
6. `docs/experiments/EXP-210-promotion-surface-lock-region-anchor-profile-snapshot.md`
7. `docs/experiments/EXP-211-prompt-baseline-coverage-audit-after-promotion-region-anchor-profiles.md`
8. `docs/experiments/EXP-212-prompt-baseline-quick-option-gap-priority-after-promotion-region-anchor-profiles.md`

## 추가 진행 9

1. 남아 있던 마지막 `P3`인 `review region emphasis` 4건을 이번 라운드에서 연속으로 닫았습니다.
2. 먼저 `EXP-213`에서 `highlightPrice=false / shorterCopy=true / emphasizeRegion=true`를 열었고, baseline/candidate single run은 둘 다 `100.0`, repeatability도 `3/3 pass`를 기록했습니다.
3. 이어서 `EXP-215`에서 `highlightPrice=false / shorterCopy=false / emphasizeRegion=true`를 열었고, 설명 길이를 늘려도 nearby/detail-location leakage 없이 `100.0`, `3/3 pass`를 유지했습니다.
4. 다음 `EXP-217`에서는 `highlightPrice=true / shorterCopy=false / emphasizeRegion=true`를 검증했고, candidate는 가격 숫자 hallucination 없이 value foreground를 유지하면서 `100.0`, repeatability `3/3 pass`를 기록했습니다.
5. 마지막 `EXP-219`에서 default review region 조합 `highlightPrice=true / shorterCopy=true / emphasizeRegion=true`도 다시 `100.0`, repeatability `3/3 pass`로 닫았습니다.
6. 네 조합 모두 `review_strict_fallback_surface_lock_*_region_anchor` profile로 manifest에 추가했고, `EXP-214`, `EXP-216`, `EXP-218`, `EXP-220` snapshot은 전부 `accepted=true`를 기록했습니다.
7. worker runtime summary는 네 조건 모두 `option_match`로 새 profile을 직접 추천했고, worker `21 passed`, api `5 passed`도 다시 확인했습니다.
8. 이후 `EXP-221`, `EXP-222`를 다시 돌린 결과 coverage는 `option_match=23`, `default_match=1`, `exact_match=24`, `recommendedAction=none`, quick option gap `0`으로 정리됐습니다.

## 추가 결과 9

1. 이제 prompt baseline quick-option catalog 24개 문맥은 모두 exact match로 연결됩니다.
2. `default_match=1`은 main baseline이고, 나머지 23개는 option profile이 직접 매칭됩니다.
3. 따라서 다음 실험은 더 이상 baseline 빈칸 메우기가 아니라, baseline 간 비교, provider 재평가, 운영 경계 확장 같은 2차 질문으로 넘어가야 합니다.

## 추가 문서 9

1. `docs/experiments/EXP-213-gpt5mini-review-region-anchor.md`
2. `docs/experiments/EXP-214-review-strict-fallback-surface-lock-region-anchor-profile-snapshot.md`
3. `docs/experiments/EXP-215-gpt5mini-review-shorter-copy-off-region-anchor.md`
4. `docs/experiments/EXP-216-review-strict-fallback-surface-lock-shorter-copy-off-region-anchor-profile-snapshot.md`
5. `docs/experiments/EXP-217-gpt5mini-review-highlight-price-shorter-copy-off-region-anchor.md`
6. `docs/experiments/EXP-218-review-strict-fallback-surface-lock-highlight-price-shorter-copy-off-region-anchor-profile-snapshot.md`
7. `docs/experiments/EXP-219-gpt5mini-review-highlight-price-region-anchor.md`
8. `docs/experiments/EXP-220-review-strict-fallback-surface-lock-highlight-price-region-anchor-profile-snapshot.md`
9. `docs/experiments/EXP-221-prompt-baseline-coverage-audit-after-review-region-anchor-profiles.md`
10. `docs/experiments/EXP-222-prompt-baseline-quick-option-gap-priority-after-review-region-anchor-profiles.md`

## 추가 진행 10

1. baseline gap이 모두 닫힌 뒤 바로 다음 질문으로, `현재 채택한 review surface-lock prompt`에서 provider 분리가 여전히 유효한지 재검토했습니다.
2. 이를 위해 `services/worker/experiments/prompt_harness.py`에 `EXP-223` model comparison을 추가했고, `fixed_review_surface_locked_exact_region_anchor` prompt를 고정한 채 `Gemma 4`와 `gpt-5-mini`만 비교했습니다.
3. `EXP-223` single run 결과는 의외로 둘 다 `100.0`이었고, strict region / nearby leakage 기준도 모두 통과했습니다.
4. 즉 `review surface-lock` 현재 prompt는 더 이상 `gpt-5-mini만 통과하는 prompt`가 아니라는 점이 확인됐습니다.
5. 다만 Gemma 4 single run latency가 `59초` 수준이라, 바로 이어서 동일 비교를 3회 반복한 `EXP-224` repeatability spot check도 추가했습니다.
6. `EXP-224` 결과는 single-run과 달리 크게 갈렸고, Gemma 4는 `0/3 timeout`, `gpt-5-mini`는 `3/3 pass`, 평균 latency 약 `5.2초`를 기록했습니다.
7. 따라서 review fallback에서 `gpt-5-mini`를 유지하는 현재 판단은 prompt 품질보다 운영 repeatability 관점에서 여전히 타당하다는 결론이 나왔습니다.

## 추가 결과 10

1. review provider 분리는 `Gemma는 아예 못 씀`이 아니라, `현재 prompt 기준 single-run 품질은 되지만 기본 transport 재현성이 부족함`으로 다시 해석하는 편이 맞습니다.
2. 즉 현재 review lane에서 진짜 차별점은 prompt 적합성보다 `운영 실용성`입니다.
3. 다음에 Gemma를 review lane에 다시 올려보려면 새 카피 실험보다 `timeout/retry transport 조건`을 붙인 운영성 검증이 먼저입니다.

## 추가 문서 10

1. `docs/experiments/EXP-223-review-surface-lock-current-baseline-model-comparison.md`
2. `docs/experiments/EXP-224-review-surface-lock-current-baseline-model-comparison-repeatability.md`

## 추가 진행 11

1. 여기까지의 카피 검토 후, 현재 병목이 더 이상 copy baseline이 아니라 영상 baseline이라는 점을 명시적으로 정리했습니다.
2. `EXP-225`로 방향 전환 문서를 추가해, 카피 축은 동결하고 이후 실험 시간을 영상 쪽으로 다시 몰아야 한다는 기준을 문서화했습니다.
3. 이 문서에서는 `멈출 것`, `계속할 것`, `다음 영상 baseline 우선순위`를 분리했고, 특히 `single-photo + phrase 미세조정` 반복은 중단 대상으로 고정했습니다.
4. 다음 active 질문은 `보존성 / motion / repeatability` 3축에서 영상 후보를 가르는 것으로 다시 정의했습니다.

## 추가 문서 11

1. `docs/experiments/EXP-225-video-baseline-reset-after-copy-freeze.md`

## 추가 진행 12

1. 영상 baseline 실험으로 바로 넘어가기 전에, 현재 방향이 서비스 기획 의도와 맞는지 다시 점검했습니다.
2. 결론은 `영상 baseline 전환은 맞지만 조건부`입니다.
3. 다음 작업은 생성형 영상 모델을 본선으로 가정하는 실험이 아니라, `본선 템플릿 숏폼 대비 생성형 영상이 보조 또는 대체 후보가 될 수 있는지`를 판정하는 baseline이어야 합니다.
4. 따라서 다음 영상 비교에는 `local LTX`, `Sora 2`, `manual Veo reference`만 놓지 말고, 반드시 `template motion + compositor` 또는 현 deterministic renderer 산출물을 product control로 포함해야 합니다.
5. `manual Veo`는 맥주 샘플에서 품질 참고점으로 강하지만, 규카츠 샘플에서 원본 보존 실패가 있었으므로 계속 `quality upper-bound reference only`로만 유지합니다.
6. single-photo 조건 안에서 prompt phrase, crop, edit만 더 미세조정하는 흐름은 `EXP-91`~`EXP-93` 결과상 수익이 작아 중단 대상으로 다시 확인했습니다.

## 추가 문서 12

1. `docs/experiments/EXP-226-current-direction-review-before-video-baseline.md`

## 추가 진행 13

1. 방향 재검토 뒤 바로 다음 작업으로, `좋은 B급`과 `실패한 생성`을 구분하는 품질 gate를 먼저 문서화했습니다.
2. `EXP-227`에서 이 프로젝트의 B급 감성을 `모델 퀄리티 경쟁`이 아니라 `의도된 과장 연출 + 원본 보존 유지`로 재정의했습니다.
3. 같은 문서에서 다음부터 고정할 판정 기준을 `원본 보존성`, `광고 체감 품질`, `반복 안정성`, `서비스 적합성` 4개로 정리했습니다.
4. 이어서 `scripts/video_upper_bound_benchmark.py`에 `product_control` provider를 추가했고, `--benchmark-id`로 별도 baseline artifact를 남길 수 있게 정리했습니다.
5. `product_control`은 생성형 video가 아니라 현재 서비스 본선 기준선을 흉내 내는 `template motion + compositor` control로 정의했습니다.
6. 이 control은 원본 사진을 유지한 채 `b_grade_fun` 오버레이, 4컷 구성, CTA를 얹어 본선 비교 기준으로 사용하도록 만들었습니다.
7. 이후 `EXP-228`로 `규카츠`, `맥주` 샘플에 대해 `product_control`, `local_ltx`, `manual_veo`를 같은 artifact root에서 다시 비교했습니다.

## 추가 결과 13

1. `product_control`은 두 샘플 모두 정상 생성됐고, 본선 기준선으로 바로 비교 가능한 상태가 됐습니다.
2. `규카츠`에서 `product_control`은 `avg_rgb_diff=0.65`, `local_ltx`는 `0.93`, `manual_veo`는 `14.55`였습니다.
3. `맥주`에서 `product_control`은 `avg_rgb_diff=0.61`, `local_ltx`는 `0.95`, `manual_veo`는 `16.94`였습니다.
4. 하지만 contact sheet를 함께 보면 `product_control`은 motion 수치가 낮아도 `컷 전환 + 오버레이 + CTA`로 광고 문법이 가장 분명했습니다.
5. 반대로 `local_ltx`는 보존성 수치는 좋지만, 두 샘플 모두 거의 한 컷 반복에 가까워 여전히 `보존형 정지 클립` 성격이 강했습니다.
6. `manual_veo`는 품질 상한선 reference로는 강했지만, 규카츠에서 QR과 주변 오브젝트 재구성이 다시 분명해 production 후보로 보긴 어렵다는 판단이 유지됐습니다.
7. 이번 비교로 `motion metric 하나만으로 광고 품질을 판단하면 안 된다`는 점도 더 명확해졌습니다.
8. 즉 현재 서비스 baseline에서 중요한 것은 `객체 motion`만이 아니라 `컷 템포`, `오버레이`, `CTA 전달`까지 포함한 광고 에너지입니다.

## 추가 문서 13

1. `docs/experiments/EXP-227-bgrade-quality-gate-and-video-baseline-scorecard.md`
2. `docs/experiments/EXP-228-product-control-video-baseline-bridge.md`

## 추가 진행 14

1. 다음 실험으로 `Sora 2 current best`를 `product_control` 기준선에 올리기 위해 benchmark 스크립트를 한 번 더 확장했습니다.
2. `scripts/video_upper_bound_benchmark.py`에 `sora2_current_best` provider를 추가했고, 현재 best-preserve 후보인 `hero_tight_zoom` 입력을 샘플별로 자동 생성하도록 만들었습니다.
3. 이번 정의에서 `current best`는 `EXP-92`와 `EXP-93` 기준으로 `hero_tight_zoom + baseline prompt` 조합으로 고정했습니다.
4. 이유는 `edit motion recovery` 계열이 motion은 조금 늘렸지만 preserve loss가 커서 본선 비교선으로 보기 어려웠기 때문입니다.
5. 이후 `EXP-229`로 `product_control`, `local_ltx`, `sora2_current_best`, `manual_veo`를 다시 같은 artifact root에서 비교했습니다.

## 추가 결과 14

1. `product_control`, `local_ltx`, `manual_veo`는 정상 완료됐고 결과 해석은 `EXP-228`과 동일했습니다.
2. 하지만 `sora2_current_best`는 `규카츠`, `맥주` 모두 `401 invalid_api_key`로 즉시 실패했습니다.
3. 즉 이번 라운드에서 확인된 병목은 `Sora current best 설계`가 아니라 현재 저장소의 `OPENAI_API_KEY` 상태입니다.
4. 다만 prepared input과 benchmark 경로는 정상 생성됐기 때문에, 키만 복구되면 같은 커맨드로 즉시 재실행 가능합니다.
5. 따라서 현재 active 결론은 `Sora 2를 실험선에 다시 올릴 구조는 준비 완료`, `live scorecard 판정은 credential 복구 후 보류`입니다.

## 추가 문서 14

1. `docs/experiments/EXP-229-sora2-current-best-vs-product-control.md`

## 추가 진행 15

1. `EXP-229`가 credential 문제로 막힌 뒤 바로 다음 축으로, 외부 모델이 아니라 본선 control 자체를 더 광고형으로 만들 수 있는지 확인했습니다.
2. `scripts/video_upper_bound_benchmark.py`에 `product_control_motion` provider를 추가했고, 각 scene image에 `ffmpeg zoompan` 기반 deterministic motion을 넣는 경로를 만들었습니다.
3. 이 경로는 생성형 video가 아니라 `template motion + compositor` uplift 실험입니다.
4. 이후 `EXP-230`으로 `product_control`, `product_control_motion`, `local_ltx`, `manual_veo`를 `규카츠`, `맥주`에서 다시 비교했습니다.

## 추가 결과 15

1. `규카츠`에서 `product_control`의 `avg_rgb_diff`는 `0.65`, `product_control_motion`은 `12.33`으로 크게 올랐고, `mid-frame MSE`는 `2360.96 -> 2677.87`로 증가폭이 제한적이었습니다.
2. `맥주`에서는 `avg_rgb_diff`가 `0.61 -> 7.43`으로 올랐고, `mid-frame MSE`는 `2490.45 -> 2514.74`로 거의 늘지 않았습니다.
3. 즉 `product_control_motion`은 두 샘플 모두 static control보다 광고 에너지를 분명히 올렸고, 특히 맥주에서는 preserve cost가 거의 없었습니다.
4. `local_ltx`는 여전히 보존형 후보였지만 near-static 성격이 강했고, `manual_veo`는 상한선 reference로는 유효하되 baseline 후보로는 여전히 어려웠습니다.
5. 이번 결과로 `template motion + compositor` 자체가 꽤 강한 본선 후보가 될 수 있다는 점이 더 선명해졌습니다.

## 추가 문서 15

1. `docs/experiments/EXP-230-product-control-motion-ovaat.md`
